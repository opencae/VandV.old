#!/bin/sh
# Source tutorial run functions
. $WM_PROJECT_DIR/bin/tools/RunFunctions

runApplication -s magU postProcess -func 'mag(U)' -latestTime
runApplication -s sample postProcess -func sample -latestTime
